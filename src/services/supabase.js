/**
 * Supabase 클라이언트 (추후 연결)
 *
 * 연결 시:
 * 1. npm install @supabase/supabase-js
 * 2. .env에 VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY 추가
 * 3. createClient로 클라이언트 생성 후 export
 */

export const supabase = null
